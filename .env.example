# Database connection string
#
# Option 1: Local MongoDB with replica set (use docker-compose.yml)
# Run: docker-compose up -d
# Note: ?replicaSet=rs0 is REQUIRED for Prisma (see MONGODB_REPLICA_SET.md)
COSMOS_DB_CONNECTION_STRING="mongodb://localhost:27017/recipe-maker-dev?replicaSet=rs0"
#
# Option 2: Azure Cosmos DB (MongoDB API) - no replica set setup needed
# COSMOS_DB_CONNECTION_STRING="mongodb://[username]:[password]@[account].mongo.cosmos.azure.com:10255/?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000&appName=@[account]@"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Authentication (Google OAuth)
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# NextAuth Secret (generate with: openssl rand -base64 32)
AUTH_SECRET="your-auth-secret-here"

# Optional: For production deployment
# NODE_ENV="production"